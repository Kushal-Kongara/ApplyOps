"""Application preparation: turning a job + an approved resume + the
candidate's own local facts into a reviewed set of answers, ready for a
human to submit — never automatically.

Named `application_prep` (not `applications`, which already exists as the
application-*tracking* module from an earlier phase) to keep "tracking a
job's status" and "preparing answers for one" clearly separate concerns.
"""
